import * as React from 'react';

const ProfileForm = () => {
    return (
        <form>
            ProfileForm
            <input type="submit" value="Next"/>
        </form>
    );
};

export default ProfileForm;
